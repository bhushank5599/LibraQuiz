server:
  port: 8088

spring:
  application:
    name: QUESTION-MANAGEMENT-SERVICE
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
