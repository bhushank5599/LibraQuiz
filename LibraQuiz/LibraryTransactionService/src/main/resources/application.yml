server:
  port: 8085

spring:
  application:
    name: LIBRARY-TRANSACTION-SERVICE
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
