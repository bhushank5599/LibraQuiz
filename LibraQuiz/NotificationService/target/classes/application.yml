server:
  port: 8093

spring:
  application:
    name: NOTIFICATION-SERVICE
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
